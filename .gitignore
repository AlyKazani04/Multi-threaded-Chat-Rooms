# exclude all files
*

# except files with extensions and directories.
# (Since, we are working on linux, executables have no extensions, this is the way to exclude them)
!*.*
!*/

# except Makefiles, they dont have extensions as well.
![Mm]akefile

# build files
build/

# vscode files
.vscode/

# Aly's lazygit file
lazygit
